{
  "name": "shopify-proxy-analytics",
  "version": "1.0.0",
  "description": "Self-hosted Shopify attribution proxy (/_cks) for headless storefronts — one-click deploy on Cloudflare Workers",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "hono": "^4.7.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.0.0",
    "typescript": "^5.8.2",
    "wrangler": "^4.0.0"
  }
}
